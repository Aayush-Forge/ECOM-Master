/**
 * Payments API client.
 * Real backend payment endpoints are pending integration.
 */

export function getAllPaymentsSync(filter = {}) {
  return [];
}

export async function getAllPayments(filter = {}) {
  return [];
}

export async function getPaymentByOrderId(orderId) {
  return null;
}
